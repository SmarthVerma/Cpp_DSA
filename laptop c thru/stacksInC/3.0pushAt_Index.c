#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>

#include <math.h>
void print(int n){      // O(logn)
    for(int i=n; i>0; i/=2){
        printf("hello smmty");
    }
7/7/7
}

void squareRootComplexity(int n) {
    int limit = sqrt(n); // Calculate the square root of n
    for (int i = 1; i <= limit; i++) {
        printf("hello smmty\n"); // Constant-time operation
    }
}
